Robo-buds - This is a small react application, in order to demonstarate the power of REACT. 

To run the project:

1. Clone this repo
2. Run `npm install`
3. Run `npm start`

(Make sure you have installed git before running the above commands)
